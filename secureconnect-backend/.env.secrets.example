# =============================================================================
# SecureConnect Backend - Secrets Configuration
# =============================================================================
# This file contains all sensitive configuration values that should be managed
# using Docker Secrets or external vault in production.
#
# SECURITY WARNING:
# - Do NOT commit this file to version control
# - Use Docker Secrets in production
# - Rotate secrets regularly
# - Use strong, randomly generated values
# =============================================================================

# =============================================================================
# JWT Configuration
# =============================================================================
# Generate a strong JWT secret (minimum 32 characters recommended)
# Command: openssl rand -base64 32
JWT_SECRET=

# =============================================================================
# Database Credentials
# =============================================================================
# CockroachDB
DB_PASSWORD=change-this-password-in-production

# Cassandra (if authentication is enabled)
CASSANDRA_PASSWORD=change-this-password-in-production

# SMTP (Email Provider)
# For production, configure SMTP to send real emails
# Leave empty to use mock sender (development mode)
SMTP_PASSWORD=change-this-password-in-production
SMTP_USERNAME=your-email@gmail.com

# Redis
REDIS_PASSWORD=change-this-password-in-production

# =============================================================================
# MinIO Configuration
# =============================================================================
# Generate strong access and secret keys
# Command: openssl rand -base64 24
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=

# =============================================================================
# Docker Secrets Reference
# =============================================================================
# To use Docker Secrets instead of environment variables:
#
# 1. Create secrets file:
#    echo "your-secret-value" | docker secret create jwt_secret -
#
# 2. Update docker-compose.yml to use secrets:
#    secrets:
#      jwt_secret:
#        external: true
#    services:
#      api-gateway:
#        secrets:
#          - jwt_secret
#        environment:
#          - JWT_SECRET_FILE=/run/secrets/jwt_secret
#
# 3. Update application code to read from _FILE variables:
#    if envVar + "_FILE" exists, read from file
#    otherwise, use envVar directly
# =============================================================================
